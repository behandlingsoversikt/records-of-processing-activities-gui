import styled from 'styled-components';

const RepresentativeItemHeader = styled.div`
  font-weight: 700;
  display: flex;
  align-items: center;
`;

const RepresentativeItemIcon = styled.div`
  display: flex;
  margin-right: .3em;
  }
`;

export default { RepresentativeItemHeader, RepresentativeItemIcon };
