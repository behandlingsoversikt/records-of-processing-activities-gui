import styled from 'styled-components';

import Common from '../common/styled';

const Headline = styled(Common.Division)``;

const Title = styled.h1`
  font-size: 30px;
  font-weight: bolder;
  margin-bottom: 5px;
`;
const SubTitle = styled.h2``;

export default { Headline, Title, SubTitle };
